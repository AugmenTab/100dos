<section
  class="tab{{#if tabs.primary.medical.active}} active{{/if}}"
  data-group="primary"
  data-tab="medical"
  role="tabpanel"
  id="{{sheetId}}-primary-panel-medical"
  aria-labelledby="{{sheetId}}-primary-tab-medical"
>
  <h2>{{localize "DOS100.pc.nav.primary.medical"}}</h2>
  {{!-- TODO: Replace with real Medical content. --}}
  <p>This page doesn't have any content yet.</p>
</section>
