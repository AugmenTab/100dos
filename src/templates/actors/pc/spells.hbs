<section
  class="tab{{#if tabs.primary.spells.active}} active{{/if}}"
  data-group="primary"
  data-tab="spells"
  role="tabpanel"
  id="{{sheetId}}-primary-panel-spells"
  aria-labelledby="{{sheetId}}-primary-tab-spells"
>
  <h2>{{localize "DOS100.pc.nav.primary.spells"}}</h2>
  {{!-- TODO: Replace with real spell-source content. Each spell grant source will get its own secondary tab in the "spellSources" tab group defined in pc-sheet.ts. --}}
  <p>This page doesn't have any content yet.</p>
</section>
