<section
  class="tab{{#if tabs.primary.effects.active}} active{{/if}}"
  data-group="primary"
  data-tab="effects"
  role="tabpanel"
  id="{{sheetId}}-primary-panel-effects"
  aria-labelledby="{{sheetId}}-primary-tab-effects"
>
  <h2>{{localize "DOS100.pc.nav.primary.effects"}}</h2>
  {{!-- TODO: Replace with real Effect management content. --}}
  <p>This page doesn't have any content yet.</p>
</section>
