<section
  class="tab{{#if tabs.primary.dashboard.active}} active{{/if}}"
  data-group="primary"
  data-tab="dashboard"
  role="tabpanel"
  id="{{sheetId}}-primary-panel-dashboard"
  aria-labelledby="{{sheetId}}-primary-tab-dashboard"
>
  <input name="name" type="text" value="{{actor.name}}" placeholder="{{localize 'DOS100.actor.name'}}" />
  <span>{{localize "TYPES.Actor.pc"}}</span>
  {{!-- TODO: Replace with the Dashboard wireframe (image, Archetype, initiative, Luck, Wounds, Fatigue, Damage Resistance, Characteristics, hotbar, pinned Skills, active Effects). --}}
  <p>This page doesn't have any content yet.</p>
</section>
