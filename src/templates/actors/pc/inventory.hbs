<section
  class="tab{{#if tabs.primary.inventory.active}} active{{/if}}"
  data-group="primary"
  data-tab="inventory"
  role="tabpanel"
  id="{{sheetId}}-primary-panel-inventory"
  aria-labelledby="{{sheetId}}-primary-tab-inventory"
>
  <h2>{{localize "DOS100.pc.nav.primary.inventory"}}</h2>
  {{!-- TODO: Replace with real Inventory content. --}}
  <p>This page doesn't have any content yet.</p>
</section>
