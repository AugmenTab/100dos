<section
  class="tab{{#if tabs.primary.features.active}} active{{/if}}"
  data-group="primary"
  data-tab="features"
  role="tabpanel"
  id="{{sheetId}}-primary-panel-features"
  aria-labelledby="{{sheetId}}-primary-tab-features"
>
  <h2>{{localize "DOS100.pc.nav.primary.features"}}</h2>
  {{!-- TODO: Replace with real Feature management content. --}}
  <p>This page doesn't have any content yet.</p>
</section>
