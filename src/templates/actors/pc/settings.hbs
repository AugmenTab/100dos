<section
  class="tab{{#if tabs.primary.settings.active}} active{{/if}}"
  data-group="primary"
  data-tab="settings"
  role="tabpanel"
  id="{{sheetId}}-primary-panel-settings"
  aria-labelledby="{{sheetId}}-primary-tab-settings"
>
  <h2>{{localize "DOS100.pc.nav.primary.settings"}}</h2>
  {{!-- TODO: Replace with real Settings/Attributes controls. --}}
  <p>This page doesn't have any content yet.</p>
</section>
