<section aria-label="{{label}}">
  <div class="dense-table-wrap">
    <table class="dense-table">
      <thead>
        <tr>
          <th scope="col" class="dense-table-flexible">{{label}}</th>
          <th scope="col">{{localize "DOS100.inventory.table.carried"}}</th>
          <th scope="col">{{localize "DOS100.inventory.table.equipped"}}</th>
          <th scope="col">{{localize "DOS100.inventory.table.weight"}}</th>
          <th scope="col">{{localize "DOS100.inventory.table.feltWeight"}}</th>
          <th scope="col">
            <a
              role="button"
              tabindex="0"
              class="icon-button"
              aria-label="{{add}}"
            ><i class="fa-solid fa-plus" inert></i></a>
          </th>
        </tr>
      </thead>
      <tbody>
        {{#if rows.length}}
          {{#each rows}}
            {{> "systems/100dos/templates/actors/pc/inventory/row-simple.hbs"}}
          {{/each}}
        {{else}}
          <tr>
            <td colspan="6" class="dense-table-empty-state">{{empty}}</td>
          </tr>
        {{/if}}
      </tbody>
    </table>
  </div>
</section>
