<section
  class="tab{{#if tabs.primary.combat.active}} active{{/if}}"
  data-group="primary"
  data-tab="combat"
  role="tabpanel"
  id="{{sheetId}}-primary-panel-combat"
  aria-labelledby="{{sheetId}}-primary-tab-combat"
>
  <h2>{{localize "DOS100.pc.nav.primary.combat"}}</h2>
  {{!-- TODO: Replace with real Combat content. --}}
  <p>This page doesn't have any content yet.</p>
</section>
