<section
  class="tab{{#if tabs.record.xp.active}} active{{/if}}"
  data-group="record"
  data-tab="xp"
  role="tabpanel"
  id="{{sheetId}}-record-panel-xp"
  aria-labelledby="{{sheetId}}-record-tab-xp"
>
  <div class="simple-stat-table">
    <div>
      <div>
        <span class="eyebrow-label">{{localize "DOS100.xp.earned"}}</span>
        <span>{{localizeNumber actor.system.xp.earned}}</span>
      </div>
      <div>
        <span class="eyebrow-label">{{localize "DOS100.xp.spent"}}</span>
        <span>{{localizeNumber actor.system.xp.spent}}</span>
      </div>
      <div>
        <span class="eyebrow-label">{{localize "DOS100.xp.available"}}</span>
        <span>{{localizeNumber actor.system.xp.available}}</span>
      </div>
      <div>
        <span class="eyebrow-label">{{localize "DOS100.xp.tier"}}</span>
        <span>{{localizeNumber actor.system.xp.tier}}</span>
      </div>
    </div>
  </div>

  <div class="ledger-table-wrap">
    <table class="ledger-table">
      <thead>
        <tr>
          <th scope="col">{{localize "DOS100.xp.ledger.type"}}</th>
          <th scope="col">{{localize "DOS100.xp.ledger.description"}}</th>
          <th scope="col" class="ledger-table-value">{{localize "DOS100.xp.ledger.value"}}</th>
          <th scope="col">{{localize "DOS100.xp.ledger.gameDate"}}</th>
          <th scope="col">{{localize "DOS100.xp.ledger.recordedBy"}}</th>
          <th scope="col">
            {{!-- TODO: not wired up yet — no Add-transaction modal exists.
                 See .local/plan.md. --}}
            <a
              role="button"
              tabindex="0"
              aria-label="{{localize "DOS100.xp.ledger.add"}}"
            ><i class="fa-solid fa-plus" inert></i></a>
          </th>
        </tr>
      </thead>
      <tbody>
        {{#if xpLedger.length}}
          {{#each xpLedger}}
            <tr>
              <td>{{localize (concat "DOS100.xp.ledger.types." this.type)}}</td>
              <td>{{this.description}}</td>
              <td class="ledger-table-value">{{localizeNumber this.value}}</td>
              <td data-tooltip="{{this.realTimeDisplay}}">
                {{#if (eq this.worldTime null)}}—{{else}}{{localizeNumber this.worldTime}}{{/if}}
              </td>
              <td>{{this.recordedByName}}</td>
              <td>
                {{!-- TODO: not wired up yet — no delete action exists.
                     See .local/plan.md. --}}
                <a
                  role="button"
                  tabindex="0"
                  aria-label="{{localize "DOS100.xp.ledger.delete" name=this.description}}"
                ><i class="fa-solid fa-trash" inert></i></a>
              </td>
            </tr>
          {{/each}}
        {{else}}
          <tr>
            <td colspan="6" class="ledger-table-empty-state">{{localize "DOS100.xp.ledger.empty"}}</td>
          </tr>
        {{/if}}
      </tbody>
    </table>
  </div>
</section>
