<section
  class="tab{{#if tabs.record.xp.active}} active{{/if}}"
  data-group="record"
  data-tab="xp"
  role="tabpanel"
  id="{{sheetId}}-record-panel-xp"
  aria-labelledby="{{sheetId}}-record-tab-xp"
>
  <h2>{{localize "DOS100.pc.nav.record.xp"}}</h2>
  {{!-- TODO: Replace with real XP ledger content. --}}
  <p>This page doesn't have any content yet.</p>
</section>
