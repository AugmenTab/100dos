<section
  class="tab{{#if tabs.record.biography.active}} active{{/if}}"
  data-group="record"
  data-tab="biography"
  role="tabpanel"
  id="{{sheetId}}-record-panel-biography"
  aria-labelledby="{{sheetId}}-record-tab-biography"
>
  <prose-mirror name="system.biography" value="{{actor.system.biography}}"></prose-mirror>
</section>
