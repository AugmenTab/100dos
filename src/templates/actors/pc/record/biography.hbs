<section
  class="tab{{#if tabs.record.biography.active}} active{{/if}}"
  data-group="record"
  data-tab="biography"
  role="tabpanel"
  id="{{sheetId}}-record-panel-biography"
  aria-labelledby="{{sheetId}}-record-tab-biography"
>
  <h2>{{localize "DOS100.pc.nav.record.biography"}}</h2>
  {{!-- TODO: Replace with real Biography editing content. --}}
  <p>This page doesn't have any content yet.</p>
</section>
