<section
  class="tab{{#if tabs.record.finances.active}} active{{/if}}"
  data-group="record"
  data-tab="finances"
  role="tabpanel"
  id="{{sheetId}}-record-panel-finances"
  aria-labelledby="{{sheetId}}-record-tab-finances"
>
  <h2>{{localize "DOS100.pc.nav.record.finances"}}</h2>
  {{!-- TODO: Replace with real Finances ledger content. --}}
  <p>This page doesn't have any content yet.</p>
</section>
