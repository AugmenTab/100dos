<hr />
<div class="dos100-form-group">
  <label for="{{sheetId}}-xp-cost">{{localize "DOS100.item.xpCost"}}</label>
  <input type="number" id="{{sheetId}}-xp-cost" name="system.xpCost" value="{{item.system.xpCost}}" step="1" />
</div>
<div class="dos100-form-group">
  <label for="{{sheetId}}-prerequisites">{{localize "DOS100.item.prerequisites"}}</label>
  <input type="text" id="{{sheetId}}-prerequisites" name="system.prerequisites" value="{{item.system.prerequisites}}" />
</div>
<div class="dos100-form-group">
  <label for="{{sheetId}}-summary">{{localize "DOS100.item.summary"}}</label>
  <input type="text" id="{{sheetId}}-summary" name="system.summary" value="{{item.system.summary}}" />
</div>
